{
	"devDependencies": {
		"esbuild": "^0.14.43"
	},
	"scripts": {
		"dev": "esbuild --watch --sourcemap=inline ModalDialog.ts --outfile=ModalDialog.js",
		"prod": "esbuild --minify ModalDialog.ts --outfile=ModalDialog.js"
	}
}
